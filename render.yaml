services:
  - type: web
    name: adaicons
    env: static
    buildCommand: echo "Static site, no build needed"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /example.html